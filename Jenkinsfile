pipeline {
    agent any

    stages {
        stage('Clone Repository') {
            steps {
                // Checkout the code from GitHub
                git url: 'https://github.com/ezimzi/public.git'
            }
        }
    }
}
